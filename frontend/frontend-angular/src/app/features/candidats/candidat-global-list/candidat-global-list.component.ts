import { Component } from '@angular/core';

@Component({
  selector: 'app-candidat-global-list',
  template: '<div class="alert alert-info">Vue CANDIDATS_GLOBALE (à brancher endpoint backend dédié).</div>'
})
export class CandidatGlobalListComponent {}